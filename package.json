{
  "name": "elm-table",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "elm-live": "npx elm-live src\\Main.elm -d dist --open -- --output=dist\\app.js",
    "build": "npx elm make src/Main.elm --output=dist/app.js",
    "release": "npx elm make src/Main.elm --output=dist/app.js --optimize"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "elm": "^0.19.1-3"
  },
  "devDependencies": {
    "elm-live": "^4.0.1"
  }
}
